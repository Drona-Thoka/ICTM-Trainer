# Environment / secrets
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Node
node_modules/

# Build output
dist/

# NOTE: ictm-reader/public/images/ and data/problems.db are deliberately
# COMMITTED. The problem bank is a separate repo that a deploy build cannot see,
# so a snapshot has to live here. Refresh with scripts/snapshot_data.py.

# Local mock DB (legacy stub backend) and SQLite sidecars
trainer.db
*.db-journal
